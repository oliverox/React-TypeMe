import TypeMe from './TypeMe';

const Delete = () => null;
Delete.defaultProps = {
  characters: 0
};

const LineBreak = () => null;

const Delay = () => null;
Delay.defaultProps = {
  ms: 0
};

export { Delete, Delay, LineBreak };
export default TypeMe;
